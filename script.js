const nav=document.querySelector('.site-header nav');const menu=document.querySelector('.mobile-menu');menu?.addEventListener('click',()=>{nav?.classList.toggle('open');menu.classList.toggle('is-open')});document.querySelectorAll('a[href]').forEach(a=>a.addEventListener('click',()=>nav?.classList.remove('open')));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.14,rootMargin:'0px 0px -8% 0px'});document.querySelectorAll('.reveal,.reveal-item,.reveal-pop,.service-card,.package-card,.value-grid article,.step').forEach(el=>observer.observe(el));document.querySelectorAll('.hero,.inner-hero').forEach(hero=>{hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;hero.style.backgroundPosition=`calc(50% + ${x*18}px) calc(50% + ${y*12}px)`;hero.style.setProperty('--mouse-x',`${(x+.5)*100}%`);hero.style.setProperty('--mouse-y',`${(y+.5)*100}%`)});hero.addEventListener('pointerleave',()=>hero.style.backgroundPosition='center')});document.querySelectorAll('.faq-list details').forEach(d=>d.addEventListener('toggle',()=>{if(d.open)document.querySelectorAll('.faq-list details').forEach(o=>{if(o!==d)o.removeAttribute('open')})}));