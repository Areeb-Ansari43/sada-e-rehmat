const nav=document.querySelector('.site-header nav');document.querySelector('.mobile-menu')?.addEventListener('click',()=>nav?.classList.toggle('open'));document.querySelectorAll('a[href]').forEach(a=>a.addEventListener('click',()=>nav?.classList.remove('open')));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal,.reveal-item,.service-card,.package-card,.value-grid article,.step').forEach(el=>observer.observe(el));document.querySelectorAll('.hero,.inner-hero').forEach(hero=>{hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5;const y=(e.clientY-r.top)/r.height-.5;hero.style.backgroundPosition=`calc(50% + ${x*12}px) calc(50% + ${y*8}px)`});hero.addEventListener('pointerleave',()=>hero.style.backgroundPosition='center')});